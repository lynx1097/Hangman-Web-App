// PLACEHOLDER DEFAULTS ONLY.
// CI (deploy-pages workflow) regenerates this file from the GitHub
// `github-pages` Environment values before building, so the real backend /
// game URLs never live in the repo.
export const environment = {
  production: true,
  apiUrl: 'https://YOUR-BACKEND-DOMAIN/api',
  gameUrl: 'https://lynx1097.github.io/Hangman-Web-App/game/',
};
