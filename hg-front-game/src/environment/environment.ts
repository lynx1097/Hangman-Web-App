export const environment = {
  production: false,
  // Laravel backend (local dev).
  apiUrl: 'http://127.0.0.1:8000/api',
  // Where to send the player after login (the Vue game, local dev).
  gameUrl: 'http://localhost:8080/',
};
